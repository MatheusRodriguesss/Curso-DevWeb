// Eventos
console.clear();
const superCalculo = () => {
  alert("Bem vindo aos Super Cálculo! ");
  console.log("Bem vindo ao Super Cálculo! ");
};
const delta = 4 * 4 - 4 * 3 * -5;

const x1 = ((-4 + Math.sqrt(delta)) / (2 * 3)); 

const x2 = ((-4 - Math.sqrt(delta)) / (2* 3));
// finalizar junto com os demais em casa