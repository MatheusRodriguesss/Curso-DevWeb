class Pessoa {
    {
        constructor (nome, dataNascimento){
            this.nome = nome;
            this.nascimento = dataNascimento;
        }
        getIdade();
        //array de pessoas com 10 posições
        //instanciar a classe pessoa em cada indice do array 
        getNiver();
        getMonth(); //retorna o mês de uma data(0 a 11)
        getDate(); //retorna o dia do mês de uma data 
    }
}